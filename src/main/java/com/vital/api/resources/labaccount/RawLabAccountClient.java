/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vital.api.resources.labaccount;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.vital.api.core.ApiError;
import com.vital.api.core.ClientOptions;
import com.vital.api.core.ObjectMappers;
import com.vital.api.core.QueryStringMapper;
import com.vital.api.core.RequestOptions;
import com.vital.api.core.VitalException;
import com.vital.api.core.VitalHttpResponse;
import com.vital.api.errors.UnprocessableEntityError;
import com.vital.api.resources.labaccount.requests.LabAccountGetTeamLabAccountsRequest;
import com.vital.api.types.GetTeamLabAccountsResponse;
import com.vital.api.types.HttpValidationError;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawLabAccountClient {
    protected final ClientOptions clientOptions;

    public RawLabAccountClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public VitalHttpResponse<GetTeamLabAccountsResponse> getTeamLabAccounts() {
        return getTeamLabAccounts(LabAccountGetTeamLabAccountsRequest.builder().build());
    }

    public VitalHttpResponse<GetTeamLabAccountsResponse> getTeamLabAccounts(RequestOptions requestOptions) {
        return getTeamLabAccounts(LabAccountGetTeamLabAccountsRequest.builder().build(), requestOptions);
    }

    public VitalHttpResponse<GetTeamLabAccountsResponse> getTeamLabAccounts(
            LabAccountGetTeamLabAccountsRequest request) {
        return getTeamLabAccounts(request, null);
    }

    public VitalHttpResponse<GetTeamLabAccountsResponse> getTeamLabAccounts(
            LabAccountGetTeamLabAccountsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/lab_test/lab_account");
        if (request.getLabAccountId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "lab_account_id", request.getLabAccountId().get(), false);
        }
        if (request.getStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "status", request.getStatus().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((key, value) -> {
                httpUrl.addQueryParameter(key, value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new VitalHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetTeamLabAccountsResponse.class),
                        response);
            }
            try {
                if (response.code() == 422) {
                    throw new UnprocessableEntityError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                            response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApiError("Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new VitalException("Network error executing HTTP request", e);
        }
    }
}
