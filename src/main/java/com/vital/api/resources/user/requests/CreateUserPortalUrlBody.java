/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vital.api.resources.user.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.vital.api.core.Nullable;
import com.vital.api.core.NullableNonemptyFilter;
import com.vital.api.core.ObjectMappers;
import com.vital.api.resources.user.types.CreateUserPortalUrlBodyContext;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreateUserPortalUrlBody.Builder.class)
public final class CreateUserPortalUrlBody {
    private final CreateUserPortalUrlBodyContext context;

    private final Optional<String> orderId;

    private final Map<String, Object> additionalProperties;

    private CreateUserPortalUrlBody(
            CreateUserPortalUrlBodyContext context,
            Optional<String> orderId,
            Map<String, Object> additionalProperties) {
        this.context = context;
        this.orderId = orderId;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return <code>launch</code>: Generates a short-lived (minutes) portal URL that is intended for launching a user from your
     * authenticated web context directly into the Junction User Portal. This URL is not suitable for asynchronous
     * communications due to its verbosity as well as short-lived nature.
     * <p><code>communications</code>: Generates a long-lived (weeks) but shortened portal URL that is suitable for Emails, SMS
     * messages and other communication channels. Users may be asked to verify their identity with Email and SMS
     * authentication, e.g., when they open a short link on a new device. ℹ️ This enum is non-exhaustive.</p>
     */
    @JsonProperty("context")
    public CreateUserPortalUrlBodyContext getContext() {
        return context;
    }

    /**
     * @return If specified, the generated URL will deeplink to the specified Order.
     */
    @JsonIgnore
    public Optional<String> getOrderId() {
        if (orderId == null) {
            return Optional.empty();
        }
        return orderId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("order_id")
    private Optional<String> _getOrderId() {
        return orderId;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateUserPortalUrlBody && equalTo((CreateUserPortalUrlBody) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreateUserPortalUrlBody other) {
        return context.equals(other.context) && orderId.equals(other.orderId);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.context, this.orderId);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static ContextStage builder() {
        return new Builder();
    }

    public interface ContextStage {
        /**
         * <p><code>launch</code>: Generates a short-lived (minutes) portal URL that is intended for launching a user from your
         * authenticated web context directly into the Junction User Portal. This URL is not suitable for asynchronous
         * communications due to its verbosity as well as short-lived nature.</p>
         * <p><code>communications</code>: Generates a long-lived (weeks) but shortened portal URL that is suitable for Emails, SMS
         * messages and other communication channels. Users may be asked to verify their identity with Email and SMS
         * authentication, e.g., when they open a short link on a new device. ℹ️ This enum is non-exhaustive.</p>
         */
        _FinalStage context(@NotNull CreateUserPortalUrlBodyContext context);

        Builder from(CreateUserPortalUrlBody other);
    }

    public interface _FinalStage {
        CreateUserPortalUrlBody build();

        /**
         * <p>If specified, the generated URL will deeplink to the specified Order.</p>
         */
        _FinalStage orderId(Optional<String> orderId);

        _FinalStage orderId(String orderId);

        _FinalStage orderId(Nullable<String> orderId);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements ContextStage, _FinalStage {
        private CreateUserPortalUrlBodyContext context;

        private Optional<String> orderId = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CreateUserPortalUrlBody other) {
            context(other.getContext());
            orderId(other.getOrderId());
            return this;
        }

        /**
         * <p><code>launch</code>: Generates a short-lived (minutes) portal URL that is intended for launching a user from your
         * authenticated web context directly into the Junction User Portal. This URL is not suitable for asynchronous
         * communications due to its verbosity as well as short-lived nature.</p>
         * <p><code>communications</code>: Generates a long-lived (weeks) but shortened portal URL that is suitable for Emails, SMS
         * messages and other communication channels. Users may be asked to verify their identity with Email and SMS
         * authentication, e.g., when they open a short link on a new device. ℹ️ This enum is non-exhaustive.</p>
         * <p><code>launch</code>: Generates a short-lived (minutes) portal URL that is intended for launching a user from your
         * authenticated web context directly into the Junction User Portal. This URL is not suitable for asynchronous
         * communications due to its verbosity as well as short-lived nature.</p>
         * <p><code>communications</code>: Generates a long-lived (weeks) but shortened portal URL that is suitable for Emails, SMS
         * messages and other communication channels. Users may be asked to verify their identity with Email and SMS
         * authentication, e.g., when they open a short link on a new device. ℹ️ This enum is non-exhaustive.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("context")
        public _FinalStage context(@NotNull CreateUserPortalUrlBodyContext context) {
            this.context = Objects.requireNonNull(context, "context must not be null");
            return this;
        }

        /**
         * <p>If specified, the generated URL will deeplink to the specified Order.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage orderId(Nullable<String> orderId) {
            if (orderId.isNull()) {
                this.orderId = null;
            } else if (orderId.isEmpty()) {
                this.orderId = Optional.empty();
            } else {
                this.orderId = Optional.of(orderId.get());
            }
            return this;
        }

        /**
         * <p>If specified, the generated URL will deeplink to the specified Order.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage orderId(String orderId) {
            this.orderId = Optional.ofNullable(orderId);
            return this;
        }

        /**
         * <p>If specified, the generated URL will deeplink to the specified Order.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "order_id", nulls = Nulls.SKIP)
        public _FinalStage orderId(Optional<String> orderId) {
            this.orderId = orderId;
            return this;
        }

        @java.lang.Override
        public CreateUserPortalUrlBody build() {
            return new CreateUserPortalUrlBody(context, orderId, additionalProperties);
        }
    }
}
